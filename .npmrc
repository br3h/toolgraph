# Keep transitive deps out of the root of node_modules (stricter, catches undeclared imports).
strict-peer-dependencies=false
auto-install-peers=true
# Reproducible installs in CI.
prefer-frozen-lockfile=true
